<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ohgiraffers.tuna.mapper.MemberMapper">


    <resultMap id="MemberResultMap" type="com.ohgiraffers.tuna.domain.Member">
        <id column="user_id" property="userId"/>
        <result column="login_id" property="loginId"/>
        <result column="password" property="password"/>
        <result column="name" property="name"/>
        <result column="personal_number" property="personalNumber"/>
        <result column="phone_number" property="phoneNumber"/>
        <result column="nickname" property="nickname"/>
    </resultMap>

    <!-- 사용자 목록 조회 -->
    <select id="findAllUsers" resultMap="MemberResultMap">
        SELECT user_id, login_id, password, name, personal_number, phone_number, nickname
        FROM tbl_user;
    </select>

</mapper>


spring:
  datasource:
    driver-class-name: org.mariadb.jdbc.Driver
    url: jdbc:mariadb://localhost:3306/gomendb
    username: gomendb
    password: gomendb

mybatis:
  type-aliases-package: com.tuna.gomen.report
  mapper-locations: classpath:mapper/*.xml

